#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../buster-static/bin/buster-static" "$@"
  ret=$?
else 
  node  "$basedir/../buster-static/bin/buster-static" "$@"
  ret=$?
fi
exit $ret
